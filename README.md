# EDP Landing (React)

Minimal React landing page for EduDiscipline Platform (EDP).

Run locally:

```bash
npm install
npm run dev
```

This project uses Vite. Open http://localhost:5173 after `npm run dev`.
